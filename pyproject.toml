[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "rppulses"
version = "0.1.0"
dependencies = ["numpy"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
rppulses = ["*.so"]